title: Classes
pages: true
  hide: true
  template:
    - classTypes
    - packages
files: true
fields:
  title:
    label: Title
    type:  text
  intro:
    label: Intro text
    type: textarea
    buttons:
      - bold
      - italic
  textReplacement:
    label: Handwritten Text Replacement
    type: structure
    entry: >
      {{text}} → <img src="/content/classes/{{image}}" />
    fields:
      text:
        label: Text to replace
        type: text
        required: true
      image:
        label: Image
        type: select
        options: images
  subpages:
    label: Classes
    type: subpagelist
